ext_if = "em0"
int_if = "em1"

# NAT для внутренней сети
nat on $ext_if from $int_if:network to any -> ($ext_if)

# Блокировать всё по умолчанию
block all

# Разрешить внутренний трафик
pass quick on $int_if
# Разрешить исходящие соединения
pass out on $ext_if
