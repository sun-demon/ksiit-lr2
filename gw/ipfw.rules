#!/bin/sh

# Clear all rules
ipfw -f flush

# Allow all on loopback
ipfw add 100 allow all from any to any via lo0

# NAT for traffic, outgoing throughout em0 (outer interface)
ipfw add 200 nat 1 ip from 192.168.1.0/24 to any out via em0

# Allow traffic in inner network (LAN)
ipfw add 300 allow all from 192.168.1.0/24 to any via em1

# Allow all other traffic (and traffic after NAT)
ipfw add 65000 allow all from any to any
